import React from "react";
import {TraceLocation} from "../api";
import {ICardProps} from "@blueprintjs/core";

export default function VisitedLocationToday({location, ...props}: { location: TraceLocation } & ICardProps) {

}